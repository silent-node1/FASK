# Image files

The pipeline image is currently connected. Suggested names:

- `teaser.png`
- `pipeline.png` (currently connected)

`teaser.png` is not connected yet because no teaser image has been supplied.
